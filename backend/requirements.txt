# IMPORTANTE: use Python 3.10, 3.11 ou 3.12.
# O TensorFlow (dependência do DeepFace) ainda NÃO suporta Python 3.13/3.14,
# e versões muito novas do Python podem não ter "wheels" prontos, forçando
# a compilação do código-fonte (que exige um compilador C/C++).
#
# As versões abaixo usam faixas (>=) para que o pip escolha wheels
# compatíveis com a sua versão do Python automaticamente.

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
opencv-python>=4.9
numpy>=1.26,<2.1
Pillow>=10.0
deepface>=0.0.93
tf-keras>=2.16
